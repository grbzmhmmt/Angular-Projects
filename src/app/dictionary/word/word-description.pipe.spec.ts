import { WordDescriptionPipe } from './word-description.pipe';

describe('WordDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new WordDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
