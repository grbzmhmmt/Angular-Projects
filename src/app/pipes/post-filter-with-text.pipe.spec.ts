import { PostFilterWithTextPipe } from './post-filter-with-text.pipe';

describe('PostFilterWithTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PostFilterWithTextPipe();
    expect(pipe).toBeTruthy();
  });
});
